# Qwen Local Assistant Status

> **Superseded by `docs/STATUS.md`** (on `chore/project-hygiene` / `integration/next`,
> merged into this branch once integration lands), which is now the single source of
> truth for status across the whole repo, not just the Qwen assistant. Treat anything
> below that contradicts it as stale.

* AVANCE-WS7 = ForgeGrid Fedora coordinator
* JParrisDesktop = Windows Ollama host
* qwen3.5:4b
* ForgeGrid server-side gateway architecture
* production LLM is advisory-only
* live DadLAN GUI architecture
* persistent history = next feature
* web research = next feature
* private remote phone access = next feature
* self-update reliability branch exists separately and is NOT deployed
* relevant source-code commit SHAs:
  - Canonical backend: 735fede
  - Qwen GUI: ee801e0
  - Self-update reliability: 33c767e
